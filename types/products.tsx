export type Product = {
  id: number | string;
  name: string;
  description: string;
  price: number | string;
  size: string;
  color: string[];
  stock: string | number;
  gender: string;
  image: any;
}